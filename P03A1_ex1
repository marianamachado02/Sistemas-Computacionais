		;1.Desenvolver	um programa que soma 3 valores. Utilizar diversas representações;
		
start	mov		r0, #0 ; registo onde vai ficar a soma
		mov		r1, #4 ; valor a ser somado
		mov		r2, #5 ; valor a ser somado
		mov		r3, #7 ; valor a ser somado
		add		r0, r1, r2
		add		r0, r0, r3
		end
